# Production-ready defaults. Override with localhost only when running the backend locally.
BACKEND_URL=https://trickee-backend-397358873357.asia-south1.run.app
NEXT_PUBLIC_BACKEND_URL=/api/backend
# Optional direct WebSocket origin. Defaults to BACKEND_URL with ws/wss protocol.
# Set this when the WebSocket service uses a different origin; do not use /api/backend.
# NEXT_PUBLIC_WS_URL=wss://trickee-backend-397358873357.asia-south1.run.app
NEXT_PUBLIC_GOOGLE_CLIENT_ID=397358873357-qfd1kdt5fhgduu4tvq92l5dg8otmh6ln.apps.googleusercontent.com
# Set to the final frontend origin for absolute metadata URLs.
# NEXT_PUBLIC_SITE_URL=https://trickee.co.in
